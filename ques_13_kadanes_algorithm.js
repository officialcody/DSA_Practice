/**
 * QUESTION: Given an array Arr[] of N integers. Find the contiguous sub-array(containing at least 
 * one number) which has the maximum sum and return its sum.
 * 
 * INPUT:
 *  N = 5
 *  Arr[] = {1,2,3,-2,5}
 * OUTPUT:
 *  9
 */

// IN Algorithms folder